# 07 最终集成回归测试与验收 - 测试文档

## 1. 测试目标

验证阶段 07：【最终集成回归测试与验收】的开发内容是否满足技术实现文档定义的目标、边界和验收标准。

## 2. 测试范围

- 执行全部阶段回归
- 执行样板 L2 需求端到端验证
- 执行安全测试
- 执行异常和回滚测试
- 检查 Evidence 完整性
- 检查低入侵要求
- 生成最终验收报告

## 3. 不测试范围

- 不新增功能
- 不做大规模架构调整
- 不接完整 Hub/Visual
- 不引入新的长期能力
- 不修改验收标准

## 4. 测试前置条件

1. 当前阶段开发已完成。
2. 前置阶段验收已通过。
3. 当前 Git 工作区状态已记录。
4. 已安装项目依赖。
5. 已确认包管理器和测试命令。
6. 如命令不适配当前项目，必须根据阶段 00 基线结果修正并记录。

## 5. 测试类型

本阶段根据实际改动覆盖以下测试类型：

- 单元测试
- 集成测试
- 回归测试
- 异常测试
- 安全测试
- 兼容性测试
- 可观测性测试
- 回滚测试

## 6. 测试用例表

| 用例编号 | 测试类型 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
|---|---|---|---|---|---|---|
| 07-C01 | 端到端测试 | 完整主链路 | 00-06 阶段通过 | 执行 init→doctor→plan→dry-run→run→verify→report | 全链路完成且证据完整 | P0 |
| 07-C02 | 安全测试 | 敏感文件保护 | 构造 .env 和 prod config | 尝试加入上下文或修改 | 被阻断或脱敏 | P0 |
| 07-C03 | 回滚测试 | 异常中断后 abort | run 进行中 | 执行 abort | worktree 和 lock 清理成功 | P0 |
| 07-C04 | 兼容性测试 | Hub/Visual 不可用 | 关闭相关 feature flags | 执行完整主链路 | 本地仍可完成 | P0 |
| 07-C05 | 质量测试 | 全量 lint/typecheck/build/test | 依赖安装完成 | 执行项目标准命令 | 无新增失败 | P0 |

## 7. 自动化测试建议

| 测试对象 | 建议位置 | 说明 |
|---|---|---|
| 新增模块单元测试 | `tests/` 或 `src/**/*.test.ts`【待确认】 | 根据当前项目测试框架确定 |
| CLI 集成测试 | `tests/cli/`【待确认】 | 使用临时目录模拟 Git 项目 |
| 文件系统测试 | `tests/fixtures/`【待确认】 | 使用 fixture 避免污染真实项目 |
| Guard 测试 | `tests/guards/`【待确认】 | 构造危险 diff 和敏感文件 |
| 回归测试 | 现有测试目录 | 确保旧功能不破坏 |

## 8. 命令行测试指令

请根据阶段 00 确认结果选择实际命令。

- `br-spec init`
- `br-spec doctor`
- `br-spec plan "新增产品配置管理模块"`
- `br-spec dry-run`
- `br-spec run`
- `br-spec verify`
- `br-spec report`
- `br-spec abort --run <runId>`
- `br-spec clean --stale`
- `br-spec resume <runId>`
- `npm test 或 pnpm test`
- `npm run lint 或 pnpm lint`
- `npm run typecheck 或 pnpm typecheck`
- `npm run build 或 pnpm build`

如果项目使用 npm：

- `npm install`
- `npm test`
- `npm run lint`
- `npm run typecheck`
- `npm run build`

如果项目使用 pnpm：

- `pnpm install`
- `pnpm test`
- `pnpm lint`
- `pnpm typecheck`
- `pnpm build`

## 9. 失败排查指南

| 失败类型 | 排查步骤 |
|---|---|
| 类型检查失败 | 查看新增类型、导出路径、tsconfig include/exclude |
| 单元测试失败 | 先确认是否为本阶段改动导致，不允许删除测试 |
| CLI 命令失败 | 查看参数解析、运行目录、配置文件路径 |
| 文件系统测试失败 | 检查临时目录、权限、路径兼容性 |
| Git 相关失败 | 检查当前是否为 Git 仓库、工作区是否 clean |
| Guard 误拦截 | 检查规则、allowlist、测试 fixture |
| Evidence 缺失 | 检查 writer 调用时机和异常处理 |
| 模型输出失败 | 检查 ModelProvider、Schema 校验和 fallback |

## 10. 测试通过标准

1. P0 测试用例全部真实执行。
2. P0 测试用例全部通过。
3. 无新增回归失败。
4. 测试命令和结果已写入验收结果文档。
5. 失败项已修复或明确标记为阻塞。
6. 不存在伪造测试结果。
