# P0.7-测试文档：Hook-Test-Repair-Evidence闭环

## 1. 测试目标

验证本阶段“实现 Hook 生命周期、测试执行、最多 2 次自动修复、Evidence Report 生成。”是否真实可用、可回归、可回滚，并确保没有越界修改和运行态污染。

## 2. 测试范围

| 测试范围 |
| ---- |
| pre-task |
| pre-edit |
| post-edit |
| pre-test |
| post-test |
| repair-hook |
| archive-hook |
| maxRepairAttempts=2 |
| Evidence Report |


## 3. 不测试范围

| 不测试范围 |
| ---- |
| 删除测试绕过失败 |
| 无限修复 |
| 把原始 Prompt 提交到 Git |


## 4. 测试前置条件

1. 当前代码分支已创建。
2. 上一阶段验收通过或已明确允许进入本阶段。
3. 依赖已安装。
4. 项目测试命令已确认；无法确认时标记【待确认】。
5. 工作区变更已记录，便于回滚。

## 5. 测试类型

| 测试类型 | 是否适用 | 说明 |
| ---- | ---- | ---- |
| 单元测试 | 适用 | 验证函数、Schema、工具方法 |
| 集成测试 | 适用 | 验证模块之间的调用和生成文件 |
| 端到端测试 | 按阶段适用 | P0.8、P1.6、P2.7、P3.7、P4.7、P5.7 必须执行 |
| 回归测试 | 适用 | 确认已有能力未破坏 |
| 权限测试 | 按阶段适用 | 涉及 Agent、RBAC、Policy 时必须执行 |
| 异常测试 | 适用 | 缺失配置、路径错误、权限不足 |
| 兼容性测试 | 适用 | 多项目、多包管理器、已有配置 |
| 性能测试 | 按阶段适用 | 大项目扫描或大量事件聚合时执行 |
| 安全测试 | 适用 | 运行态污染、密钥泄露、越权修改 |
| 可观测性测试 | 按阶段适用 | 涉及事件和报告时执行 |
| AI 模型调用测试 | 不作为强依赖 | P0-P1 不要求真实模型调用 |
| 回滚测试 | 适用 | 验证回滚方案有效 |


## 6. 测试用例表

| 用例编号 | 测试类型 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| P0.7-TC01 | 单元测试 | pre-task | 前置阶段已完成，当前环境可执行 | 执行与“pre-task”相关的命令或文件校验 | pre-task 满足预期且无越界修改 | P0 |
| P0.7-TC02 | 集成测试 | pre-edit | 前置阶段已完成，当前环境可执行 | 执行与“pre-edit”相关的命令或文件校验 | pre-edit 满足预期且无越界修改 | P0 |
| P0.7-TC03 | 异常测试 | post-edit | 前置阶段已完成，当前环境可执行 | 执行与“post-edit”相关的命令或文件校验 | post-edit 满足预期且无越界修改 | P0 |
| P0.7-TC04 | 回归测试 | pre-test | 前置阶段已完成，当前环境可执行 | 执行与“pre-test”相关的命令或文件校验 | pre-test 满足预期且无越界修改 | P0 |
| P0.7-TC05 | 安全测试 | post-test | 前置阶段已完成，当前环境可执行 | 执行与“post-test”相关的命令或文件校验 | post-test 满足预期且无越界修改 | P0 |
| P0.7-TC06 | 回滚测试 | repair-hook | 前置阶段已完成，当前环境可执行 | 执行与“repair-hook”相关的命令或文件校验 | repair-hook 满足预期且无越界修改 | P0 |
| P0.7-TC07 | 单元测试 | archive-hook | 前置阶段已完成，当前环境可执行 | 执行与“archive-hook”相关的命令或文件校验 | archive-hook 满足预期且无越界修改 | P0 |
| P0.7-TC08 | 集成测试 | maxRepairAttempts=2 | 前置阶段已完成，当前环境可执行 | 执行与“maxRepairAttempts=2”相关的命令或文件校验 | maxRepairAttempts=2 满足预期且无越界修改 | P0 |
| P0.7-TC09 | 异常测试 | Evidence Report | 前置阶段已完成，当前环境可执行 | 执行与“Evidence Report”相关的命令或文件校验 | Evidence Report 满足预期且无越界修改 | P0 |


## 7. 自动化测试建议

1. Schema 校验放在 `tests/schema/` 或现有测试目录【待确认】。
2. CLI 命令测试放在 `tests/cli/` 或 `__tests__/cli/`【待确认】。
3. 生成文件快照测试放在 `tests/fixtures/`。
4. Mock 策略：禁止真实写入用户 home，可通过临时目录模拟 `~/.ai-spec-auto/`。
5. 边界场景：目录已存在、配置已存在、字段缺失、checksum 不匹配、测试命令失败。
6. 异常场景：无写权限、路径包含空格、项目名称为空、Git 不存在。
7. 测试命名规则：`阶段编号-能力-场景`，例如 `P0.7-manifest-missing-field`。

## 8. 命令行测试指令

以下命令需由执行模型根据项目实际技术栈修正，不得伪造执行结果。

```bash
npm run lint 或 pnpm lint【待确认】\nnpm run typecheck 或 pnpm typecheck【待确认】\nnpm test 或 pnpm test【待确认】\nnpm run build 或 pnpm build【待确认】\nbr-spec status【如 CLI 已实现】\nbr-spec check --stage P0.7【如 CLI 已实现】
```

## 9. 失败排查指南

1. 先看命令输出。
2. 再看变更文件。
3. 再看配置文件。
4. 再看日志。
5. 再看依赖版本。
6. 再看兼容性问题。
7. 不允许删除测试绕过失败。
8. 不允许降低测试标准。
9. 如果无法判断，记录【待确认】并标记阻塞。

## 10. 测试通过标准

1. 本阶段所有高优先级用例通过。
2. 必要命令执行结果为成功，或失败原因明确且不阻塞。
3. 未发现运行态数据进入项目 Git。
4. 未发现密钥、原始 Prompt、敏感日志进入 Git。
5. 回滚测试可执行或已明确记录【待确认】。
