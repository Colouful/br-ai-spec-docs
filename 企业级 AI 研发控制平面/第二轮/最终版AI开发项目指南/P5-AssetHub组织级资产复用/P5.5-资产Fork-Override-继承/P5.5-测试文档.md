# P5.5-测试文档：资产Fork-Override-继承

## 1. 测试目标

验证本阶段“实现企业资产、团队 Fork、项目 Override 和继承冲突检测。”是否真实可用、可回归、可回滚，并确保没有越界修改和运行态污染。

## 2. 测试范围

| 测试范围 |
| ---- |
| inheritance tree |
| fork record |
| override rule |
| conflict detection |


## 3. 不测试范围

| 不测试范围 |
| ---- |
| 随意覆盖安全底线规则 |


## 4. 测试前置条件

1. 当前代码分支已创建。
2. 上一阶段验收通过或已明确允许进入本阶段。
3. 依赖已安装。
4. 项目测试命令已确认；无法确认时标记【待确认】。
5. 工作区变更已记录，便于回滚。

## 5. 测试类型

| 测试类型 | 是否适用 | 说明 |
| ---- | ---- | ---- |
| 单元测试 | 适用 | 验证函数、Schema、工具方法 |
| 集成测试 | 适用 | 验证模块之间的调用和生成文件 |
| 端到端测试 | 按阶段适用 | P0.8、P1.6、P2.7、P3.7、P4.7、P5.7 必须执行 |
| 回归测试 | 适用 | 确认已有能力未破坏 |
| 权限测试 | 按阶段适用 | 涉及 Agent、RBAC、Policy 时必须执行 |
| 异常测试 | 适用 | 缺失配置、路径错误、权限不足 |
| 兼容性测试 | 适用 | 多项目、多包管理器、已有配置 |
| 性能测试 | 按阶段适用 | 大项目扫描或大量事件聚合时执行 |
| 安全测试 | 适用 | 运行态污染、密钥泄露、越权修改 |
| 可观测性测试 | 按阶段适用 | 涉及事件和报告时执行 |
| AI 模型调用测试 | 不作为强依赖 | P0-P1 不要求真实模型调用 |
| 回滚测试 | 适用 | 验证回滚方案有效 |


## 6. 测试用例表

| 用例编号 | 测试类型 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| P5.5-TC01 | 单元测试 | inheritance tree | 前置阶段已完成，当前环境可执行 | 执行与“inheritance tree”相关的命令或文件校验 | inheritance tree 满足预期且无越界修改 | P1 |
| P5.5-TC02 | 集成测试 | fork record | 前置阶段已完成，当前环境可执行 | 执行与“fork record”相关的命令或文件校验 | fork record 满足预期且无越界修改 | P1 |
| P5.5-TC03 | 异常测试 | override rule | 前置阶段已完成，当前环境可执行 | 执行与“override rule”相关的命令或文件校验 | override rule 满足预期且无越界修改 | P1 |
| P5.5-TC04 | 回归测试 | conflict detection | 前置阶段已完成，当前环境可执行 | 执行与“conflict detection”相关的命令或文件校验 | conflict detection 满足预期且无越界修改 | P1 |


## 7. 自动化测试建议

1. Schema 校验放在 `tests/schema/` 或现有测试目录【待确认】。
2. CLI 命令测试放在 `tests/cli/` 或 `__tests__/cli/`【待确认】。
3. 生成文件快照测试放在 `tests/fixtures/`。
4. Mock 策略：禁止真实写入用户 home，可通过临时目录模拟 `~/.ai-spec-auto/`。
5. 边界场景：目录已存在、配置已存在、字段缺失、checksum 不匹配、测试命令失败。
6. 异常场景：无写权限、路径包含空格、项目名称为空、Git 不存在。
7. 测试命名规则：`阶段编号-能力-场景`，例如 `P5.5-manifest-missing-field`。

## 8. 命令行测试指令

以下命令需由执行模型根据项目实际技术栈修正，不得伪造执行结果。

```bash
npm run lint 或 pnpm lint【待确认】\nnpm run typecheck 或 pnpm typecheck【待确认】\nnpm test 或 pnpm test【待确认】\nnpm run build 或 pnpm build【待确认】\nbr-spec status【如 CLI 已实现】\nbr-spec check --stage P5.5【如 CLI 已实现】
```

## 9. 失败排查指南

1. 先看命令输出。
2. 再看变更文件。
3. 再看配置文件。
4. 再看日志。
5. 再看依赖版本。
6. 再看兼容性问题。
7. 不允许删除测试绕过失败。
8. 不允许降低测试标准。
9. 如果无法判断，记录【待确认】并标记阻塞。

## 10. 测试通过标准

1. 本阶段所有高优先级用例通过。
2. 必要命令执行结果为成功，或失败原因明确且不阻塞。
3. 未发现运行态数据进入项目 Git。
4. 未发现密钥、原始 Prompt、敏感日志进入 Git。
5. 回滚测试可执行或已明确记录【待确认】。
