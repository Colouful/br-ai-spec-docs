# 00.2-跨仓库协议清单冻结-技术实现文档

## 1. 阶段目标

基于当前 br-ai-spec 已有实现，冻结后续 Visual / Hub 需要对接的 Schema 与 Fixture。

## 2. 目标仓库

`br-ai-spec`

## 3. 本阶段范围

整理 Run Event、Evidence、Manifest、Lock、AssetPackage、AgentProfile 等协议清单。

## 4. 不在本阶段范围内的内容

不实现 Visual 页面；不实现 Hub 后台；不修改业务逻辑。

## 5. 前置条件

1. 已阅读根目录 `README.md`。
2. 已阅读 `00-多仓库职责边界说明.md`。
3. 已确认当前仓库为 `br-ai-spec`。
4. 已创建本阶段独立开发分支。
5. 如依赖其他仓库输出，必须使用本包提供的 Schema / Fixture，不允许自行臆造协议。

## 6. 相关模块与文件

重点查看 schemas、types、contracts、manifest、lock、evidence、events 相关文件。

如果无法确定具体文件，请基于项目结构定位并在进度同步文档中标记【待确认】。

## 7. 当前问题

1. `br-ai-spec` 已经执行过 P0-P5，但 Visual 和 Hub 仓库尚未完成对应主体能力。
2. P4 / P5 不能继续在单仓库中闭门实现。
3. 后续必须先完成边界收口，再补连接器、Visual 观测、Hub 资产中心和四仓联调。

## 8. 目标设计

1. 当前阶段职责只落在指定仓库。
2. 与其他仓库通过明确 Schema / API / Fixture 对接。
3. 变更可测试、可验收、可回滚。
4. 不污染业务项目，不存储敏感数据。
5. 所有真实测试和验收结果必须由执行模型在执行后填写。

## 9. 技术实现方案

### 9.1 模块设计

围绕 `跨仓库协议清单冻结` 建立最小可用实现，优先复用现有模块；不存在时再新增最小实现。

### 9.2 接口设计

涉及跨仓库时优先使用本包 `05-跨仓库协议与样例数据/` 下的 Schema。接口必须保持稳定、向后兼容。

### 9.3 数据结构

所有结构化数据必须包含版本字段、时间字段、来源字段和兼容策略。严禁无版本裸结构跨仓库传输。

### 9.4 配置结构

配置必须支持默认值。敏感配置必须来自环境变量或用户本地配置，不允许写入 Git。

### 9.5 文件结构

新增文件必须放在本阶段允许范围内；如果现有项目已有同类目录，必须复用。

### 9.6 调用流程

读取配置 → 校验输入 → 执行业务逻辑 → 输出结构化结果 → 写日志 / 事件 → 返回结果。

### 9.7 异常处理

必须区分参数错误、权限错误、依赖不可用、数据不兼容、执行失败和未知错误。

### 9.8 兼容策略

新增字段必须可选；删除字段必须保留兼容读取；接口变更必须支持旧版本最小读取。

### 9.9 回滚策略

所有新增文件可通过 Git 回滚；数据库变更必须有迁移回滚说明；配置变更必须能恢复默认值。

### 9.10 日志要求

日志必须记录阶段编号、仓库、操作、结果、错误摘要，不记录密钥、原始 Prompt、完整源码。

### 9.11 权限要求

涉及写操作、发布、回滚、审计、上报时必须进行权限或配置校验。

### 9.12 安全要求

必须脱敏敏感字段；不得上传源码；不得存储原始 Prompt；不得绕过 Hook / Policy / Test。

### 9.13 测试影响范围

测试必须覆盖本阶段新增或修改的核心流程，至少包含成功路径、失败路径和回滚路径。


## 字段与 JSON 结构要求

### RunEvent

| 字段名 | 字段类型 | 是否必填 | 默认值 | 字段说明 | 示例值 | 校验规则 | 兼容策略 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| eventId | string | 是 | 无 | 事件唯一 ID | evt_001 | 非空 | 新增字段必须可选 |
| runId | string | 是 | 无 | 运行任务 ID | run_20260507_001 | 非空 | 跨版本保持稳定 |
| projectId | string | 是 | 无 | 项目 ID | br-ai-spec-a83f91c2 | 非空 | 与 Local State 一致 |
| eventType | string | 是 | 无 | 事件类型 | hook.finished | 枚举 | 新增枚举向后兼容 |
| stage | string | 是 | 无 | 阶段 | post-test | 非空 | 未知阶段允许展示为其他 |
| status | string | 是 | unknown | 状态 | success | success/failure/running/skipped/unknown | 新增状态默认 unknown |
| severity | string | 否 | info | 严重程度 | blocking | info/warn/error/blocking | 默认 info |
| message | string | 否 | 空字符串 | 事件摘要 | 测试通过 | 长度限制 | 敏感信息脱敏 |
| timestamp | string | 是 | 当前时间 | ISO 时间 | 2026-05-07T00:00:00Z | ISO8601 | 保持 UTC |
| metadata | object | 否 | {} | 扩展信息 | {} | 必须可 JSON 序列化 | 不可放源码/密钥/原始 Prompt |

JSON 示例：

```json
{
  "eventId": "evt_001",
  "runId": "run_20260507_001",
  "projectId": "br-ai-spec-a83f91c2",
  "eventType": "hook.finished",
  "stage": "post-test",
  "status": "success",
  "severity": "info",
  "message": "post-test 检查通过",
  "timestamp": "2026-05-07T00:00:00Z",
  "metadata": {
    "hookId": "post-test",
    "durationMs": 1200
  }
}
```

### EvidenceReport

| 字段名 | 字段类型 | 是否必填 | 默认值 | 字段说明 | 示例值 | 校验规则 | 兼容策略 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| runId | string | 是 | 无 | 运行 ID | run_001 | 非空 | 稳定 |
| projectId | string | 是 | 无 | 项目 ID | demo-a83f91c2 | 非空 | 稳定 |
| taskId | string | 否 | 空 | 任务 ID | task_001 | 可空 | 旧版本可无 |
| specId | string | 否 | 空 | Spec ID | spec_001 | 可空 | 旧版本可无 |
| changedFiles | array | 否 | [] | 变更文件摘要 | [] | 不得包含完整源码 | 只保留路径和摘要 |
| testResults | array | 否 | [] | 测试结果 | [] | 结构化 | 兼容旧字段 |
| hookResults | array | 否 | [] | Hook 结果 | [] | 结构化 | 兼容旧字段 |
| repairResults | array | 否 | [] | 修复结果 | [] | 结构化 | 兼容旧字段 |
| reviewResults | array | 否 | [] | Review 结果 | [] | 结构化 | 兼容旧字段 |
| finalStatus | string | 是 | unknown | 最终状态 | success | success/failure/blocked/unknown | 新增枚举向后兼容 |

JSON 示例：

```json
{
  "runId": "run_20260507_001",
  "projectId": "br-ai-spec-a83f91c2",
  "taskId": "task_001",
  "specId": "spec_001",
  "changedFiles": [
    {
      "path": "src/index.ts",
      "changeType": "modify",
      "summary": "增加事件上报"
    }
  ],
  "testResults": [
    {
      "command": "pnpm test",
      "status": "success"
    }
  ],
  "hookResults": [
    {
      "hookId": "post-test",
      "status": "success"
    }
  ],
  "repairResults": [],
  "reviewResults": [],
  "finalStatus": "success"
}
```

### AssetPackage

| 字段名 | 字段类型 | 是否必填 | 默认值 | 字段说明 | 示例值 | 校验规则 | 兼容策略 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| assetId | string | 是 | 无 | 资产 ID | rule.react.basic | 非空 | 稳定 |
| assetType | string | 是 | 无 | 资产类型 | rule | rule/skill/agentProfile/workflow/hook/command | 新增类型向后兼容 |
| name | string | 是 | 无 | 资产名称 | React 基础规范 | 非空 | 稳定 |
| version | string | 是 | 0.1.0 | 语义化版本 | 1.0.0 | semver | 保持语义化 |
| source | string | 是 | hub | 来源 | skill-q-platform | 非空 | 稳定 |
| checksum | string | 是 | 无 | 内容校验 | sha256:xxx | sha256 | 用于防篡改 |
| compatibility | object | 否 | {} | 兼容信息 | {} | 可选 | 旧版本忽略 |
| files | array | 是 | [] | 资产文件列表 | [] | 路径合法 | 不包含运行态数据 |
| metadata | object | 否 | {} | 扩展信息 | {} | 可 JSON 序列化 | 不可含敏感信息 |

JSON 示例：

```json
{
  "assetId": "rule.react.basic",
  "assetType": "rule",
  "name": "React 基础规范",
  "version": "1.0.0",
  "source": "skill-q-platform",
  "checksum": "sha256:example",
  "compatibility": {
    "adapters": [
      "cursor",
      "claudeCode"
    ],
    "frameworks": [
      "react"
    ]
  },
  "files": [
    {
      "path": "rules/react-basic.md",
      "checksum": "sha256:file"
    }
  ],
  "metadata": {
    "owner": "platform-team"
  }
}
```

### Manifest

| 字段名 | 字段类型 | 是否必填 | 默认值 | 字段说明 | 示例值 | 校验规则 | 兼容策略 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| version | string | 是 | 0.1.0 | Manifest 版本 | 0.1.0 | semver | 稳定 |
| rules | array | 否 | [] | Rule 引用 | [] | 数组 | 兼容旧版本 |
| skills | array | 否 | [] | Skill 引用 | [] | 数组 | 兼容旧版本 |
| agentProfiles | array | 否 | [] | Agent Profile 引用 | [] | 数组 | 兼容旧版本 |
| commands | array | 否 | [] | Command 引用 | [] | 数组 | 兼容旧版本 |
| hooks | array | 否 | [] | Hook 引用 | [] | 数组 | 兼容旧版本 |
| adapters | object | 是 | {} | IDE 适配配置 | {} | 对象 | 新增适配器默认 false |
| checksum | string | 否 | 空 | 整体校验 | sha256:xxx | sha256 | 可选 |
| generatedAt | string | 是 | 当前时间 | 生成时间 | 2026-05-07T00:00:00Z | ISO8601 | 稳定 |

JSON 示例：

```json
{
  "version": "0.1.0",
  "rules": [
    {
      "assetId": "rule.react.basic",
      "version": "1.0.0"
    }
  ],
  "skills": [],
  "agentProfiles": [],
  "commands": [],
  "hooks": [],
  "adapters": {
    "cursor": true,
    "claudeCode": true,
    "codex": false
  },
  "checksum": "sha256:example",
  "generatedAt": "2026-05-07T00:00:00Z"
}
```



## 10. 具体开发任务表

| 任务编号 | 任务名称 | 任务描述 | 修改范围 | 新增文件 | 修改文件 | 优先级 | 前置依赖 | 完成标准 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 00.2.T1 | 整理事件协议 | 确认 Run Event、Hook Result、Test Result、Repair Result 字段 | 协议与类型定义 | 协议清单文档 | 必要时补充类型导出 | P0 | 00.1 | Visual 能据此开发 Collector API |
| 00.2.T2 | 整理资产协议 | 确认 Manifest、Lock、AssetPackage 字段 | 协议与类型定义 | 协议清单文档 | 必要时补充类型导出 | P0 | 00.1 | Hub 能据此开发资产接口 |
| 00.2.T3 | 生成 Fixture | 生成供 Visual / Hub 联调用的样例 JSON | fixtures 或 docs 目录 | fixtures/run-event.json 等 | 无 | P0 | 事件协议和资产协议 | Fixture 可被测试读取并通过 JSON 校验 |

## 11. 小任务清单

| 清单编号 | 所属任务 | 操作内容 | 目标文件 | 输入 | 输出 | 验证方式 | 完成状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 00.2.T1.1 | 00.2.T1 | 查找现有事件类型 | 协议与类型定义 | 当前仓库与本阶段文档 | 代码/配置/文档变更 | 执行测试文档命令 | 未开始 |
| 00.2.T1.2 | 00.2.T1 | 对齐本文档 RunEvent Schema | 协议与类型定义 | 当前仓库与本阶段文档 | 代码/配置/文档变更 | 执行测试文档命令 | 未开始 |
| 00.2.T1.3 | 00.2.T1 | 输出差异清单 | 协议与类型定义 | 当前仓库与本阶段文档 | 代码/配置/文档变更 | 执行测试文档命令 | 未开始 |
| 00.2.T2.1 | 00.2.T2 | 查找 manifest/lock 类型 | 协议与类型定义 | 当前仓库与本阶段文档 | 代码/配置/文档变更 | 执行测试文档命令 | 未开始 |
| 00.2.T2.2 | 00.2.T2 | 对齐本文档 AssetPackage Schema | 协议与类型定义 | 当前仓库与本阶段文档 | 代码/配置/文档变更 | 执行测试文档命令 | 未开始 |
| 00.2.T2.3 | 00.2.T2 | 输出差异清单 | 协议与类型定义 | 当前仓库与本阶段文档 | 代码/配置/文档变更 | 执行测试文档命令 | 未开始 |
| 00.2.T3.1 | 00.2.T3 | 生成 run event 样例 | fixtures 或 docs 目录 | 当前仓库与本阶段文档 | 代码/配置/文档变更 | 执行测试文档命令 | 未开始 |
| 00.2.T3.2 | 00.2.T3 | 生成 evidence 样例 | fixtures 或 docs 目录 | 当前仓库与本阶段文档 | 代码/配置/文档变更 | 执行测试文档命令 | 未开始 |
| 00.2.T3.3 | 00.2.T3 | 生成 asset package 样例 | fixtures 或 docs 目录 | 当前仓库与本阶段文档 | 代码/配置/文档变更 | 执行测试文档命令 | 未开始 |

## 12. AI 编程模型执行提示词

你现在执行【00.2：跨仓库协议清单冻结】。

请先阅读以下文档：

1. 本技术实现文档
2. 本阶段测试文档
3. 本阶段验收结果文档
4. 本阶段进度同步文档
5. `00-多仓库职责边界说明.md`
6. `05-跨仓库协议与样例数据/` 中与本阶段相关的 Schema

你的任务是：

1. 理解本阶段目标和边界。
2. 只修改 `br-ai-spec` 仓库中本阶段允许修改的文件和模块。
3. 按照技术实现文档完成开发。
4. 按照测试文档执行测试。
5. 将真实测试结果写入验收结果文档。
6. 更新进度同步文档。
7. 判断是否满足进入下一阶段的条件。

测试命令参考：

- `pnpm test`
- `pnpm build`

严格禁止扩大范围、跳过测试、删除测试、伪造结果、修改无关模块、引入未说明的大型依赖、破坏向后兼容、把运行态数据写入项目仓库、提交密钥/原始 Prompt/敏感日志。

完成后请输出：修改文件清单、实现摘要、执行的测试命令、测试结果、验收结果、进度同步结果、遗留问题、是否建议进入下一阶段。


## 禁止事项

1. 不允许把 Visual UI、Asset Hub 后台、企业管理页面继续塞进 `br-ai-spec`。
2. 不允许在 `br-ai-spec-visual` 中实现 CLI、Harness 执行器或资产安装逻辑。
3. 不允许在 `skill-q-platform` 中存储业务源码、原始 Prompt、密钥、敏感日志。
4. 不允许跳过测试、删除测试、降低测试标准或伪造测试结果。
5. 不允许静默忽略失败，所有失败必须记录到验收结果文档和进度同步文档。
6. 不允许破坏现有接口兼容性；如必须变更，必须提供兼容层和迁移说明。
7. 不允许引入未说明的大型依赖。
8. 不允许把运行态数据写入业务项目仓库。
9. 不允许跨仓库随意修改；每个小阶段只能修改当前文档允许的仓库与文件。
10. 不允许提交密钥、原始 Prompt、敏感日志、完整源码快照。


## 14. 风险与回滚方案

| 风险 | 触发条件 | 影响范围 | 回滚方式 | 回滚验证命令 |
| --- | --- | --- | --- | --- |
| 仓库边界混乱 | 在错误仓库实现能力 | 后续联调失败 | 回滚越界提交，按职责拆分 | `git diff --name-only` |
| Schema 不兼容 | Visual / Hub 无法消费 br-ai-spec 输出 | 跨仓库链路失败 | 回退到上一个兼容 Schema | 执行 Fixture 校验 |
| 测试失败 | 新增逻辑破坏现有功能 | 当前仓库功能异常 | 回滚本阶段分支提交 | 执行测试文档命令 |
| 敏感信息泄露 | 日志或事件包含密钥、Prompt、源码 | 安全风险 | 删除泄露文件并重写脱敏逻辑 | 执行安全测试用例 |

## 15. 完成后的输出物

1. 代码变更：待执行
2. 配置变更：待执行
3. 文档变更：待执行
4. 测试结果：待执行
5. 验收记录：待执行
6. 进度同步记录：待执行
