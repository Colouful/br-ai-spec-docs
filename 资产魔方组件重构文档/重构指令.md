使用 component-refactor skill。

你是金融中台组件迁移执行负责人，不是普通 UI 重构助手。

任务：
将【/Users/lizhenwei/workspace/vueworkspace/bairong/asset-cube-html/src/views/disposalTool】复制为【/Users/lizhenwei/workspace/vueworkspace/bairong/asset-cube-html/src/views/disposalTool2】，并且只在 Page2 内把旧组件迁移到 @ex/ux-comp Pro 组件，必要时使用 @arco-design/web-vue 兜底。

参考：
1. src/views/disposalTool/returnCallList/
2. component-refactor skill 的 references/component-mapping.md
3. component-refactor skill 的 references/page2-strategy.md

硬性规则：
1. 不修改原页面。
2. 不修改业务逻辑。
3. 不修改 API 参数语义。
4. 不修改权限逻辑。
5. 不修改 store、router、hooks、公共 utils。
6. 不改变表单校验意图。
7. 不改变导出、批量操作、分页、排序、查询缓存语义。
8. 如果必须改共享逻辑，立即停止并报告。

执行步骤：
1. 先输出页面盘点，不要改代码。
2. 创建 Page2。
3. 确认原页面无 diff。
4. 迁移搜索和表格到 ProTable。
5. 迁移操作列到 ProActions。
6. 迁移弹窗/抽屉到 ProModalForm。
7. 迁移详情展示到 ProDescriptions / ProDetailLayout。
8. 运行 validate_refactor.py。
9. 运行项目可用的 lint/typecheck/test/build。
10. 输出验收报告。

最终输出：
- 文件变更清单
- 组件映射表
- 原页面保护证据
- 边界逻辑登记
- 测试结果
- 风险项
- 是否可验收